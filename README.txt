When running the program, only open one file. Restart program to make sure figures are accurate.

To run unit tests from cgp1-prep folder:
./test/tilertest --test "All tests"

To run unit tests from cgp1-prep folder:
./tesselate/tessviewer

